The best solution is to use bucket sort. because the ages are integers from 1 - 100 (or a little bigger). We can create an integer array (int ages[100])and scan over all the records in the file and allocate each record into their bucket. 
